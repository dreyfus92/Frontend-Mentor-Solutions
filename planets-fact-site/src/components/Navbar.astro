---
const navItems = [
  { name: "VENUS", color: "decoration-[#EDA249]" },
  { name: "EARTH", color: "decoration-[#6D2ED5]" },
  { name: "MARS", color: "decoration-[#D14C32]" },
  { name: "JUPITER", color: "decoration-[#D83A34]" },
  { name: "SATURN", color: "decoration-[#CD5120]" },
  { name: "URANUS", color: "decoration-[#1EC1A2]" },
  { name: "NEPTUNE", color: "decoration-[#2D68F0]" },
];
---

<nav
  class="text-white border-b-[1px] border-b-[#979797] flex md:flex-col md:justify-around justify-between h-[68px] md:h-[159px] lg:h-[85px] lg:flex lg:flex-row lg:justify-between items-center"
>
  <h2 class="text-[32px] ml-[32px] md:ml-0 lg:ml-[32px]">The Planets</h2>
  <div
    class="md:flex md:gap-x-[33px] md:w-full md:justify-center lg:flex lg:gap-[33px] lg:w-[665px] lg:mr-[40px] hidden"
  >
    <a href="/">
      <p
        class={Astro.url.pathname === "/" ||
        Astro.url.pathname === "/structure" ||
        Astro.url.pathname === "/surface"
          ? `underline decoration-4 decoration-[#2D68F0] underline-offset-[-45px] text-[#FFFFFF] text-[11px]`
          : "text-[#FFFFFF] text-[11px]"}
      >
        MERCURY
      </p>
    </a>
    {
      navItems.map(({ name, color }) => (
        <a href={`/${name.toLowerCase()}/overview`}>
          <p
            class={
              Astro.url.pathname === `/${name.toLocaleLowerCase()}/overview` ||
              Astro.url.pathname === `/${name.toLocaleLowerCase()}/structure` ||
              Astro.url.pathname === `/${name.toLocaleLowerCase()}/surface`
                ? `underline decoration-4 ${color} underline-offset-[-45px] text-[#FFFFFF] text-[11px]`
                : "text-[#FFFFFF] text-[11px]"
            }
          >
            {name}
          </p>
        </a>
      ))
    }
  </div>
  <button class="mr-[24px] md:hidden lg:hidden xl:hidden"
    ><img src="/assets/icon-hamburger.svg" class="focus:fill-[#979797]" />
  </button>
</nav>
